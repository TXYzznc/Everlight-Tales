# 验收：b17 红舞鞋专属规则与关卡配置

## 验收结论：通过

- Play Mode 综合探针 `B17AcceptanceRunner`：**21/21 通过**（探针已删除）。
- 编译：删除探针后 `debug_force_recompile` → success=true、errorCount=0、warningCount=0。
- 程序集布局 `verify_project_assemblies.py`：通过；框架纯度 `audit_framework_purity.py`：通过。

## 覆盖映射

| 任务 | 断言 | 结果 |
|---|---|---|
| P2-013 | 导流格+1、转向格切换 D4、前方受阻束缚+1、出界束缚+1、分离后不加负荷 | 通过 |
| P2-014 | 6 枚初盘零件/封存匣/机械臂/三轮/耗时落位、完整导流路线按序命中、导流6分离、封存口未开启挡回、封存匣修好、封存完成、三轮目标+整关完成（样张可通） | 通过 |

## 备注

- 验收中发现并修复：D-051 字面坐标含 (-2,-2)（轴向距离 4），「半径 3/n=4」口径容不下，取最小合法边长 n=5（半径 4）后全部坐标合法；出界探针相应改用 (0,4)→(0,5)。
- 舞步方向本批为可变运行时状态（转向格覆盖），「旋转盘面改变可达路线」的定势联动、RS 与零件结算/推移/机械臂联动、表现层留 b18 与后续批次。
