# 验证：b20 时段显示 + 时间推进 + 昼夜刷新供给

## 验证命令

1. `python tools/unity_meta.py`（补齐新增 .meta）
2. `python tools/verify_project_assemblies.py`（五层 asmdef 引用方向）
3. `python tools/audit_framework_purity.py`（框架纯度）
4. UnitySkills：`debug_force_recompile` → `/compile/status` 0 错 0 警 → `editor_play` → `console_get_logs`

## 探针用例（B20AcceptanceRunner，Play Mode，24/24 通过）

### P3-007 时间推进 + 昼夜切换
- 每段 4 格；同段推进 2 格；恰好用完整段跨到下午；跨 2 段到夜晚；深夜跨日到次日上午
- 上午/下午=白天，夜晚/深夜=夜晚；时段显示名

### P3-008 昼夜刷新与普通事件供给
- 首批 6 候选；白天批抽满 4；夜班送还（白天权重 0）不进白天批；隔板（S2）在 S1 不出现
- 同种子复现同批次；夜晚批抽满 4；台灯（夜晚权重 0 且无夜间时段）不进夜晚批
- S2 阶段隔板可进入白天批；同批次模板无放回不重复
- 昼夜交替新批全未处理且昼夜归属正确（未处理旧实例退出、不计失败）

### P3-006 时段显示与时间盘
- 时段条左侧「第3天 · 下午」；右侧「剩2/4」；四格横向；当前时段高亮（白天色）；夜晚格弱化（夜晚色）；深夜高亮

## 结果

- 探针 24/24 通过，探针文件及 .meta 验收后删除。
- 最终重编译 0 错 0 警。
- 静态检查（meta/程序集/纯度）全部通过。

## 迭代记录

- 首跑 22/24：两处探针断言修正——(1)「深夜跨日」剩余格应为 1（非 3）；(2)「未处理退出」原用 `InstanceId` 判重，但 InstanceId 为批内序号（批间会重复），改为校验新批全未处理且昼夜归属正确。修正后 24/24。
