# 验证：b19 事件架构与案件状态机 + 费用结构 + 地图 + 序章

## 验证命令

1. `python tools/unity_meta.py`（补齐新增 .meta）
2. `python tools/verify_project_assemblies.py`（五层 asmdef 引用方向）
3. `python tools/audit_framework_purity.py`（框架纯度）
4. UnitySkills：`debug_force_recompile` → `/compile/status` 0 错 0 警 → `editor_play` → `console_get_logs`

## 探针用例（B19AcceptanceRunner，Play Mode，35/35 通过）

### P3-001 案件状态机 + 分层
- 案件初始未触发；未触发→调查中→待维修→维修中
- 失败/撤退→待维修；多关成功→调查中（关次+1）；单关全完成→已解决（计批次）
- 已解决→待回访→已回访（标记交付）；终态不可再转移；未触发不可直达已解决
- 对象分层：世界存档聚合案件/任务/地图 + 事件实例准备态 + 维修尝试类型存在

### P3-002 费用结构
- 基准 20 维修费/格；1/2/3 格 = 20/40/60
- 卷帘门 2 格组装 40 费 + 材料；怪谈修正 120 费

### P3-003 地图节点
- 开局解锁/未发现/已知未开放三态；剧情解锁；已解锁不再降级；可处理判定

### P3-004 地图操作
- 只渲染已解锁与已知未开放（未发现不出现）；点击选中；拖动更新视口并重排；等待推进到下一时段

### P3-005 序章
- 从第一帧开始；按时长推进字幕；跳过；播完完成；页绑定序列

## 结果

- 探针 35/35 通过，探针文件及 .meta 验收后删除。
- 最终重编译 0 错 0 警。
- 静态检查（meta/程序集/纯度）全部通过。
