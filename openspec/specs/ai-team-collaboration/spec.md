# AI Team Collaboration Specification

## Purpose

定义领域无关、可初始化的多窗口协作契约，涵盖派发、占用、通信、暂停恢复、增量归档和人类权威计划边界，而不预设任何产品实例。

## Requirements

### Requirement: 框架提供可初始化的多窗口协作制品

框架 MUST 在 `Docs/Framework/Development/Dispatch/` 提供领域无关的调度说明、角色路由、派发模板、占用模板、暂停恢复协议、窗口自动化规范、本机注册表示例与初始化指南。基线 MUST NOT 包含活动派发、真实占用或产品实例。

#### Scenario: 新项目初始化协作
- **WHEN** 项目采用框架协作机制
- **THEN** 制作人 MUST 能仅依据初始化指南创建本机注册表、空占用表和第一份派发单

### Requirement: 派发和占用遵守单一协调入口

制作人窗口 MUST 独占维护中央占用表；派发单 MUST 声明可写范围、高冲突文件、工具独占和 Git 索引短锁。其他窗口 MUST NOT 修改中央占用表，并 MUST 在未取得索引锁时避免暂存、提交、变基、合并、切换分支或重写历史。

#### Scenario: 并行窗口共享工作区
- **WHEN** 实施窗口准备提交已派发范围内的改动
- **THEN** 它 MUST 取得 Git 索引短锁、显式暂存授权路径并在提交后释放该锁

### Requirement: 跨窗口通信只请求制作人动作

自动派发 MUST 只发送最小启动信息，并在注册表无效、项目不匹配或发送失败时降级为手动派发。专业窗口 MUST 仅在阻塞、权限或锁申请/释放、跨职能依赖、阶段放行、中央状态更新、增量归档或暂停需要制作人处理时回传。

#### Scenario: 普通实施进度
- **WHEN** 专业窗口的进度不需要制作人采取动作
- **THEN** 该窗口 MUST 保留进度在本窗口且不得发送跨窗口广播

### Requirement: 暂停与恢复保留可交接现场

协作规范 MUST 定义局部、批次和项目级暂停，以及 S0 至 S2 抢占严重度、最小交接信息和恢复门禁。暂停或抢占 MUST 不扩大派发范围或自动改变人类权威计划。

#### Scenario: 共享工具冲突
- **WHEN** 窗口发现 Unity、DCC、目录或高冲突文件占用冲突
- **THEN** 它 MUST 暂停相关工作、保留现场并请求制作人协调，而不是接管或覆盖其他窗口改动

### Requirement: 权威计划与可编辑输入边界可配置

派发模板和初始化说明 MUST 参数化表达由人类或外部系统维护的权威计划、只读输入和 AI 可编辑输入，且 MUST NOT 硬编码工作簿、任务编号或产品路径。

#### Scenario: 项目声明外部计划
- **WHEN** 项目使用框架模板声明权威计划
- **THEN** 模板 MUST 允许项目填写计划来源和只读边界而不把该计划复制到框架基线

### Requirement: 专业窗口在派发边界内高自治

专业窗口 MUST 在已派发工作、可写路径、已确认方案和验收边界内自主讨论、实施、验证和增量归档。职责内的用户决定 MUST 由提问窗口记录并直接同步受影响窗口。普通进度、单项决定、测试通过、增量归档和独立提交 MUST NOT 通知制作人。

#### Scenario: 职责内用户决定影响另一个窗口
- **WHEN** 专业窗口获得会影响另一个职能的用户决定
- **THEN** 提问窗口 MUST 直接通知受影响窗口，而不经制作人转述

### Requirement: 制作人只接收强制升级与批次验收

制作人 MUST 只处理范围、优先级、负责人、排期、OpenSpec边界或依赖变化；点对点无法解决的冲突；共享资源或仓库异常；新授权；S0/S1暂停；以及整个派发批次完成。除这些事件外，窗口 MUST NOT 发送仅供知悉的制作人消息。

#### Scenario: 普通独立提交完成
- **WHEN** Git 集成窗口完成一个专业窗口的独立提交
- **THEN** 它 MUST 仅向请求窗口回传结果，且不得通知制作人
